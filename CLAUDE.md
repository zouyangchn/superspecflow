# SuperSpecFlow

OpenSpec + Superpowers project integration toolkit.

## Available Skills

- `superspec-single` — Single project / monorepo integration (propose → apply → archive)
- `superspec-multi` — Multi-repo integration (propose → apply → archive)

## Quick Start

For single/monorepo projects: use `/superspec-single`
For multi-repo projects: use `/superspec-multi`
